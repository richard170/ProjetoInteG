<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Auto SOS"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoBancoDados"
        tools:targetApi="31">

        <activity
            android:name=".AtvPrincipalEscolha"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AtvPrincipal" />
        <activity android:name=".AtvLoginAdmin" />
        <activity android:name=".AtvLoginUsuario" />
        <activity android:name=".AtvListaVeiculo"></activity>
        <activity android:name=".AtvCadVeiculo" />
        <activity android:name=".util.ChatManager" />
        <activity android:name=".AtvEscolhaChat" />
        <activity android:name=".AtvChatUsuario" />

    </application>

</manifest>
