package com.example.projetobancodados;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class AtvStatusUsuario extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.atv_status_usuario);

        // Adicione lógica para exibir informações ou realizar ações específicas para o status do usuário
    }
}
